<script webc:type="js" webc:is="template">
  collections[collection].map((post) => {
    return `<div>
      <p><big>${post.data.title}</big></p>
      <p>${post.data.description}</p>
      <a href="${post.url}">Read post</a>
      </div>`;
  }).join("");
</script>