<p>&copy; <span webc:nokeep @html="copyrightYear(firstInProduction)"></span> <span webc:nokeep @text="orgName"></span></p>

<div @html="number('3') - 2"></div>

<p webc:nokeep @html="loop(collections.all, (item) => `
  <p>${item.data.title}</p>
  <div>${item.data.description}</div>`
)">
</p>