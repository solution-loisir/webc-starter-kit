<nav>
  <ul role="list" class="flex gap-400">
    <script webc:type="js">
      dataSource.map((item) => `<li><a href="${item.page.url}"${item.page.url === $data.page.url ? ` aria-current="page"` : ""}>${item.data.navigation.label}</a></li>`).join("\n");
    </script>
  </ul>
</nav>